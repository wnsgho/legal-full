# ===========================================
# Docker Compose 환경변수 설정
# 이 파일을 .env로 복사하고 값을 수정하세요
# ===========================================

# ===========================================
# 필수 설정 (반드시 변경 필요)
# ===========================================
# OpenAI API 키
OPENAI_API_KEY=your_openai_api_key_here

# ===========================================
# Neo4j Desktop 설정 (로컬)
# ===========================================
# Neo4j Desktop에서 설정한 비밀번호
NEO4J_PASSWORD=your_neo4j_password_here
NEO4J_USER=neo4j
NEO4J_DATABASE=neo4j
# Docker 컨테이너에서 호스트 머신의 Neo4j에 접근
NEO4J_URI=neo4j://host.docker.internal:7687

# ===========================================
# 선택적 설정 (기본값 사용 가능)
# ===========================================
# OpenAI 설정
OPENAI_BASE_URL=https://api.openai.com/v1
DEFAULT_MODEL=gpt-4.1-2025-04-14
DEFAULT_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# 서버 설정
DEBUG=false
LOG_LEVEL=INFO

# 프론트엔드 설정
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_TITLE=계약서 분석 AI
# Docker에서 호스트의 Neo4j에 접근하려면 host.docker.internal 사용
VITE_NEO4J_URI=bolt://host.docker.internal:7687

